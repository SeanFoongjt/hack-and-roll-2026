/** @type {const} */
const themeColors = {
  // Zen Color Palette - Calming and peaceful colors
  primary: { light: '#6B9FB1', dark: '#5CB85C' }, // Lake (light) / Zen Garden (dark)
  background: { light: '#F5F1ED', dark: '#1A1A1A' }, // White Dove (light) / Basalt Stones (dark)
  surface: { light: '#FFFFFF', dark: '#2A2A2A' }, // Pure white cards (light) / Dark surface (dark)
  foreground: { light: '#1A1A1A', dark: '#F5F1ED' }, // Basalt Stones (light) / White Dove (dark)
  muted: { light: '#9B8B88', dark: '#B8B8B8' }, // Natural Stone (light) / River Stone (dark)
  border: { light: '#D1D5D8', dark: '#4A4A4A' }, // Mist (light) / Dark border (dark)
  accent: { light: '#D4A5A0', dark: '#D4A5A0' }, // Sunset Sky - warm accent
  success: { light: '#5CB85C', dark: '#6FD46F' }, // Zen Garden
  warning: { light: '#C9A86A', dark: '#D4B87A' }, // Wood tones
  error: { light: '#D4807A', dark: '#E89B95' }, // Orchid-inspired error
};

module.exports = { themeColors };
